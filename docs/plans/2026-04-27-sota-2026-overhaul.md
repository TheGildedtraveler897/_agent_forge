# Plan: SOTA 2026 Agent Forge Overhaul

- **Spec:** [`docs/specs/2026-04-27-sota-2026-overhaul.md`](../specs/2026-04-27-sota-2026-overhaul.md)
- **Architectural plan:** `~/.claude/plans/6-7-system-effervescent-pillow.md` (operator-approved 2026-04-27)
- **Execution mode:** sequential (single-agent in-session)
- **Total tasks:** 11
- **Estimated effort:** 25–40 minutes for the implementation; verification is operator-driven post-approval.

## Subsystem scope

Single subsystem: Agent Forge canonical authoring layer + governed-skill catalog. No code changes to renderers, verifier, or triad validator (the schema already supports every shape required).

## Task list

### T-01 — Create `skills/global/token-optimizer/SKILL.md`
Files: `skills/global/token-optimizer/SKILL.md`
Dependencies: none
Verification: file exists, frontmatter parses (`python3 -c "import yaml,sys; yaml.safe_load(open('skills/global/token-optimizer/SKILL.md').read().split('---')[1])"`).

### T-02 — Create `skills/global/prompt-auto-activator/SKILL.md`
Files: `skills/global/prompt-auto-activator/SKILL.md`
Dependencies: none
Verification: file exists; frontmatter `targets` is `["claude", "codex"]` (no Gemini).

### T-03 — Create `skills/global/prompt-auto-activator/auto-activator.sh`
Files: `skills/global/prompt-auto-activator/auto-activator.sh`
Dependencies: T-02
Verification: file exists with mode 0755; `bash -n` parses; smoke test `printf '/caveman' | bash auto-activator.sh` prints one JSON advisory line and exits 0.

### T-04 — Append `prompt-auto-activator` record to `policies/hooks.json` `shared`
Files: `policies/hooks.json`
Dependencies: T-03
Verification: `python3 -c "import json; d=json.load(open('policies/hooks.json')); ids=[r['id'] for r in d['shared']]; assert 'prompt-auto-activator' in ids" && echo OK`.

### T-05 — Append `## Checkpoint Discipline` to `skills/global/context-engineer/SKILL.md`
Files: `skills/global/context-engineer/SKILL.md`
Dependencies: none
Verification: `grep -q '^## Checkpoint Discipline' skills/global/context-engineer/SKILL.md`.

### T-06 — Append `## Checkpoint Discipline` to `skills/global/execution-planner/SKILL.md` and add Phase 5 bullet
Files: `skills/global/execution-planner/SKILL.md`
Dependencies: none
Verification: `grep -q '^## Checkpoint Discipline' skills/global/execution-planner/SKILL.md` and `grep -q 'dev/active/<slug>/' skills/global/execution-planner/SKILL.md`.

### T-07 — Append `dev/` to `.gitignore`
Files: `.gitignore`
Dependencies: none
Verification: `grep -qx 'dev/' .gitignore`.

### T-08 — Append two test methods to `tests/test_hooks_v3.py`
Files: `tests/test_hooks_v3.py`
Dependencies: T-04
RED test: `python3 -m unittest tests.test_hooks_v3.HookV3RenderingTests.test_user_prompt_submit_renders_for_claude_and_codex` fails with `KeyError` or assertion before T-04 lands; passes after.
GREEN steps:
  1. Add `test_user_prompt_submit_renders_for_claude_and_codex` asserting the new id appears under `UserPromptSubmit` in both Claude and Codex payloads.
  2. Add `test_user_prompt_submit_filtered_from_gemini` asserting no Gemini payload key contains the new id.
Verification: `python3 -m unittest tests.test_hooks_v3 -v` exits 0 with five tests OK.

### T-09 — Append lesson entry to `docs/LESSONS_LEARNED.md`
Files: `docs/LESSONS_LEARNED.md`
Dependencies: T-04
Verification: `grep -q 'user_prompt_submit Is Claude+Codex-Only' docs/LESSONS_LEARNED.md`.

### T-10 — Archive the relayed brief verbatim at `docs/specs/2026-04-27-sota-2026-overhaul.md`
Files: `docs/specs/2026-04-27-sota-2026-overhaul.md`
Dependencies: none
Verification: file exists; first H1 is `# Spec: SOTA 2026 Agent Forge Overhaul`.

### T-11 — Write this plan file at `docs/plans/2026-04-27-sota-2026-overhaul.md`
Files: `docs/plans/2026-04-27-sota-2026-overhaul.md`
Dependencies: T-10
Verification: file exists; `## Task list` section present.

## Pre-emit audit

- **Spec coverage:** every acceptance criterion in `docs/specs/2026-04-27-sota-2026-overhaul.md` maps to at least one task above (criteria 1→T-01,T-02; 2→T-04; 3→T-05,T-06; 4→T-08; 5→T-09; 6→T-07; 7,8,9→operator verification block below).
- **Placeholder scan:** no `TBD`, `...`, `fixme` in the plan body.
- **Type consistency:** function and skill names match across spec, plan, and skill bodies.
- **Independence check:** all tasks share the same in-session executor; no parallel-agent contention concern.

## Operator verification (post-implementation)

Run from repo root:

1. `python3 scripts/omni_factory.py render-registry` → exit 0; `registry.json` contains `token-optimizer` and `prompt-auto-activator`.
2. `python3 -m unittest tests.test_hooks_v3 -v` → 5 OK.
3. `python3 scripts/verify-agent-forge.py` → exit 0.
4. `python3 scripts/validate-triad-runtime.py --project jarvis` → `overall_pass: true`; per-host `hook_pass: true`.
5. (Operator-run sync) `python3 scripts/omni_factory.py --project <each governed project>`.
6. (Optional) `live-hook-prober` against Claude with a prompt containing `/caveman`; advisory line appears in trace.

No commit. No push.

## Handoff

Sequential executor: same in-session agent. No subagent dispatch.
